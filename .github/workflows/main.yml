name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: run bash script
        shell: bash
        run: |
          cd /Users/harta/Desktop/Git_repo/Harish_Project/.github
          $PWD